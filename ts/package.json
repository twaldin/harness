{
  "name": "@twaldin/harness-ts",
  "version": "0.2.5",
  "type": "module",
  "description": "Harness: CLI command construction and output parsing for AI coding agents",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=node",
    "prepare": "bun build src/index.ts --outdir=dist --target=node",
    "prepublishOnly": "bun build src/index.ts --outdir=dist --target=node",
    "test": "bun test"
  },
  "dependencies": {
    "better-sqlite3": "^12.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/bun": "latest"
  }
}
